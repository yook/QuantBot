name: 🪟 Build and Release QuantBot (Windows)

on:
  release:
    types: [published]

permissions:
  contents: write # нужно для загрузки файлов в релиз

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      # 1️⃣ Клонируем репозиторий
      - name: 🧾 Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Устанавливаем Node.js (та же версия, что в dev)
      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.x"

      # 3️⃣ Кешируем node_modules, чтобы ускорить установку зависимостей
      - name: 🧰 Cache node_modules
        uses: actions/cache@v4
        id: node-cache
        with:
          path: |
            node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      # 4️⃣ Кеш Electron и Electron Builder (внутренние бинарные зависимости)
      - name: 💾 Cache Electron builder
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/electron
            ~/.cache/electron-builder
          key: ${{ runner.os }}-electron-cache

      # 5️⃣ Устанавливаем зависимости (если не восстановились из кеша)
      - name: 📦 Install dependencies (safe)
        if: steps.node-cache.outputs.cache-hit != 'true'
        run: |
          Write-Host "🧹 Cleaning node_modules and lock file..."
          Remove-Item -Recurse -Force node_modules -ErrorAction SilentlyContinue
          Remove-Item -Force package-lock.json -ErrorAction SilentlyContinue
          Write-Host "📦 Installing dependencies..."
          npm install --include=optional --force
        shell: pwsh

      # 6️⃣ Сборка portable-версии QuantBot
      - name: 🏗️ Build portable Windows app
        run: npm run build:win:portable:cross -- --publish never

      # 7️⃣ Собираем артефакты (.exe, .yml, .blockmap) для загрузки
      - name: 🧩 Collect release artifacts
        run: |
          Write-Host "📁 Collecting artifacts..."
          New-Item -ItemType Directory -Force -Path dist-upload | Out-Null
          Copy-Item -Path "release/**/*.exe" -Destination "dist-upload" -Force -ErrorAction SilentlyContinue
          Copy-Item -Path "release/**/*.yml" -Destination "dist-upload" -Force -ErrorAction SilentlyContinue
          Copy-Item -Path "release/**/*.blockmap" -Destination "dist-upload" -Force -ErrorAction SilentlyContinue
        shell: pwsh

      # 8️⃣ Загружаем файлы в GitHub Release
      - name: 🚀 Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist-upload/**
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
