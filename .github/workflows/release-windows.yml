name: 🪟 Build and Release QuantBot (Windows)

on:
  release:
    types: [published]
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write # нужно для загрузки файлов в релиз

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      # 1️⃣ Клонируем репозиторий
      - name: 🧾 Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Устанавливаем Node.js (та же версия, что в dev)
      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.x"

      # 2.1️⃣ Устанавливаем версию из Git-тега (только если package.json отличается)
      - name: 🔖 Set version from Git tag
        run: |
          $tagName = "${{ github.ref_name }}"
          $version = $tagName.Substring(1) # Удаляем префикс "v"
          Write-Host "Tag name: $tagName -> target version: $version"

          # Читаем текущий package.json и сравниваем версию
          try {
            $pkg = Get-Content -Raw -Path package.json | ConvertFrom-Json
          } catch {
            Write-Error "Не удалось прочитать package.json: $_"
            exit 1
          }

          if ($pkg.version -eq $version) {
            Write-Host "package.json already has version $version — skipping npm version"
          } else {
            Write-Host "Updating package.json version to $version"
            npm version $version --no-git-tag-version
          }
        shell: pwsh

      # 3️⃣ Кешируем node_modules, чтобы ускорить установку зависимостей
      - name: 🧰 Cache node_modules
        uses: actions/cache@v4
        id: node-cache
        with:
          path: |
            node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      # 4️⃣ Кеш Electron, Electron Builder и Vite
      - name: 💾 Cache Electron builder and Vite
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/electron
            ~/.cache/electron-builder
            node_modules/.vite
          key: ${{ runner.os }}-build-cache-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-build-cache-

      # 5️⃣ Устанавливаем зависимости (читая lockfile для воспроизводимости)
      - name: 📦 Install dependencies (ci)
        if: steps.node-cache.outputs.cache-hit != 'true'
        run: |
          Write-Host "📦 Installing dependencies using npm ci..."
          npm ci --prefer-offline --no-audit --no-fund
        shell: pwsh

      # 5.1️⃣ Пересборка нативных модулей для Electron (sqlite modules)
      - name: 🔧 Rebuild native modules for Electron (sqlite)
        run: |
          Write-Host "🔁 Rebuilding native modules for Electron (win x64)..."

          # Set alternative cache and mirror locations for Electron headers
          $env:ELECTRON_CACHE = "$env:RUNNER_TEMP\electron-cache"
          $env:ELECTRON_CUSTOM_DIR = "37.10.0"
          $env:ELECTRON_MIRROR = "https://registry.npmmirror.com/-/binary/electron/"

          # Create cache directory
          New-Item -ItemType Directory -Force -Path $env:ELECTRON_CACHE | Out-Null

          # Retry logic for electron-rebuild with different strategies
          $maxRetries = 2
          $attempt = 1

          while ($attempt -le $maxRetries) {
            try {
              Write-Host "Attempt $attempt/$maxRetries - Rebuilding native modules..."
              
              if ($attempt -eq 1) {
                # First attempt: use npmmirror with cache
                npx electron-rebuild -f -w better-sqlite3 -w @vscode/sqlite3 --cache-dir=$env:ELECTRON_CACHE
              } else {
                # Second attempt: force rebuild with verbose logging
                npx electron-rebuild -f -w better-sqlite3 -w @vscode/sqlite3 --force --verbose --cache-dir=$env:ELECTRON_CACHE
              }
              
              Write-Host "✅ Native modules rebuilt successfully!"
              break
            }
            catch {
              Write-Host "❌ Attempt $attempt failed: $_"
              $attempt++
              
              if ($attempt -le $maxRetries) {
                Write-Host "⏳ Waiting 10 seconds before retry..."
                Start-Sleep -Seconds 10
              } else {
                Write-Host "❌ All rebuild attempts failed. Trying fallback approach..."
                
                # Fallback: try to install pre-built binaries or skip rebuild
                try {
                  Write-Host "🔄 Attempting to use pre-built better-sqlite3..."
                  npm rebuild better-sqlite3 @vscode/sqlite3 --verbose
                  Write-Host "✅ Fallback rebuild succeeded!"
                } catch {
                  Write-Host "⚠️ Native rebuild failed, but continuing build (app may use bundled SQLite fallback)"
                }
              }
            }
          }

          # Ensure step does not fail due to non-zero LASTEXITCODE from internal tools
          if ($LASTEXITCODE -ne 0) { $global:LASTEXITCODE = 0 }
        shell: pwsh

      # Workaround: Установка нативной зависимости Rollup для Windows
      - name: Install Rollup native for Windows (workaround npm optional deps bug)
        run: |
          Write-Host "Installing @rollup/rollup-win32-x64-msvc to avoid optional deps issue"
          npm i @rollup/rollup-win32-x64-msvc --no-save
        shell: pwsh

      # 6️⃣ Сборка portable-версии QuantBot
      - name: 🏗️ Build portable Windows app
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
          VITE_CI_BUILD: "true"
        run: npm run build:win:portable:ci -- --publish never

      # 7️⃣ Собираем артефакты (.exe, .yml, .blockmap) для загрузки
      - name: 🧩 Collect release artifacts
        run: |
          Write-Host "📁 Collecting artifacts..."
          New-Item -ItemType Directory -Force -Path dist-upload | Out-Null
          Copy-Item -Path "release/**/*.exe" -Destination "dist-upload" -Force -ErrorAction SilentlyContinue
          Copy-Item -Path "release/**/*.yml" -Destination "dist-upload" -Force -ErrorAction SilentlyContinue
          Copy-Item -Path "release/**/*.blockmap" -Destination "dist-upload" -Force -ErrorAction SilentlyContinue
        shell: pwsh

      # 8️⃣ Загружаем файлы в GitHub Release
      - name: 🚀 Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist-upload/**
        env:
          # Prefer a dedicated PAT if present, otherwise use the auto-provided GITHUB_TOKEN
          GH_TOKEN: ${{ secrets.GH_PAT || secrets.GITHUB_TOKEN }}

      - name: Validate GH token (safe)
        # Do not print the token; only report status and scopes.
        env:
          GH_TOKEN: ${{ secrets.GH_PAT || secrets.GITHUB_TOKEN }}
        run: |
          if (-not $env:GH_TOKEN) {
            Write-Output "GH_TOKEN_STATUS=missing"
            Write-Output "No GH token configured (GH_PAT or GITHUB_TOKEN) — publish/upload will be skipped"
          } else {
            try {
              $resp = Invoke-WebRequest -UseBasicParsing -Method Head -Uri 'https://api.github.com/user' -Headers @{ Authorization = "token $env:GH_TOKEN" } -ErrorAction Stop
              $status = $resp.StatusCode
              $scopes = $resp.Headers['X-OAuth-Scopes'] -join ', '
            } catch {
              $status = 0
              $scopes = ''
            }
            Write-Output "GH_TOKEN_STATUS=$status"
            if ($scopes) { Write-Output "GH_TOKEN_SCOPES=$scopes" }
            Write-Output "Token HTTP status: $status"
          }
